class Lives {
    constructor(root) {

        // this.lives = []

        // this.lifeCount = document.createElement('span');
        // root.appendChild(this.lifeCount);
        // this.lifeCount.id = "lives";

        this.burgerLife = document.createElement('img');
        root.appendChild(this.burgerLife);
        this.burgerLife.src = "images/player.png";


    }
}